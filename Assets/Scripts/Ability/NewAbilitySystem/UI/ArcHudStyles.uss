/* ArcHudStyles.uss */

.arc-hud-panel {
    /* Anchors the panel to a specific size, typically expanding to fill the container 
       so the corner calculations align with the screen edges. */
    position: absolute;
    width: 100%;
    height: 100%;
    overflow: hidden;
}

/* 1. ROOT PIVOT: Handles positioning and the inward translate animation */
.spell-slot {
    position: absolute;
    width: 60px;
    height: 60px;
    /* Keeps the slot perfectly centered on your math coordinates */
    translate: -50% -50%;
    transition-property: translate;
    transition-duration: 0.2s;
    transition-timing-function: ease-out-back;
}

.ability-icon {
    position: absolute;
    width: 100%;
    height: 100%;

    /* Graphics Optimization: Forces the sprite to fit cleanly within the generated quad's UVs */
    -unity-background-scale-mode: scale-to-fit;

    /* Keeps the image tucked cleanly inside the visual container's rounded borders */
    border-radius: 10px;
}\

/* The physical pivot still moves inward along its rotated local Y-axis */
.spell-slot:hover, .spell-slot.selected {
    translate: -50% -15px;
}

/* 2. VISUAL CONTAINER: Handles the upright look, colors, borders, and scaling */
.spell-slot-visual {
    width: 100%;
    height: 100%;
    background-color: rgba(30, 30, 30, 0.8);
    border-radius: 12px;
    border-width: 2px;
    border-color: #A0A0A0;

    transition-property: scale, background-color, border-color;
    transition-duration: 0.2s;
    transition-timing-function: ease-out-back;
}

/* Route the root's hover/selected states to trigger visual changes on the upright child */
.spell-slot:hover .spell-slot-visual {
    scale: 1.15;
    background-color: rgba(60, 60, 60, 0.9);
    border-color: #FFFFFF;
}

.spell-slot.selected .spell-slot-visual {
    scale: 1.15;
    border-color: #f39c12;
}

.ammo-display {
    position: absolute;
    width: 120px;
    height: 50px;
    background-color: rgba(10, 10, 10, 0.9);
    border-radius: 25px;
    border-width: 2px;
    border-color: #ffaa00;

    /* Crucial: Centers the pivot */
    translate: -50% -50%;

    align-items: center;
    justify-content: center;
}

.ammo-display > #ammo-text {
    color: #ffaa00;
    font-size: 24px;
    -unity-font-style: bold;
    -unity-text-align: middle-center;
}

/* --- 1. THE STRUCTURAL BUTTON (White Circle) --- */
.skip-turn-button {
    position: absolute;
    width: 60px;
    height: 60px;
    border-radius: 30px;
    overflow: hidden;

    /* Centers the child icon perfectly inside the circle */
    align-items: center;
    justify-content: center;

    /* Base Styling */
    background-color: rgba(255, 255, 255, 0.95);
    border-width: 2px;
    border-color: #ffaa00;

    transition-property: scale, background-color, border-color;
    transition-duration: 0.15s;
    transition-timing-function: ease-out-back;
}

/* --- 2. THE FOREGROUND ICON (Black Hourglass) --- */
.skip-turn-icon {
    /* Set to 60% so the icon breathes nicely inside the button padding */
    width: 60%;
    height: 60%;

    -unity-background-scale-mode: scale-to-fit;
    /* Tints your transparent PNG graphic to a dark slate color */
    -unity-background-image-tint-color: #1A1A1A;
}

/* --- 3. INTERACTION STATES --- */
.skip-turn-button:hover {
    scale: 1.15;
    background-color: rgba(230, 230, 230, 1);
    border-color: #ffaa00;
}

.skip-turn-button:active {
    scale: 0.95;
    background-color: rgba(200, 200, 200, 1);
    border-color: #cc8800;
}